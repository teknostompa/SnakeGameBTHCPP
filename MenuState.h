#pragma once

#include "GameState.h"
class MenuState : public GameState {
public:
    MenuState(void) { // std::string settings
        //TODO set settings
    }

    ~MenuState() {

    }

    void KeyEvent(sf::Keyboard::Key key) {
    }

    void tick() {
    }

    void Draw(sf::RenderWindow& window) {
    }
private:

};
